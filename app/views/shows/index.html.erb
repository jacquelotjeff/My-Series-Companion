<p id="notice"><%= notice %></p>

<h1>Liste des séries</h1>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Overview</th>
      <th>Banner</th>
      <th>Poster</th>
      <th>Runtime</th>
      <th>Network</th>
      <th>Rating</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @shows.each do |show| %>
      <tr>
        <td><%= show.name %></td>
        <td><%= show.overview %></td>
        <td><%= show.banner %></td>
        <td><%= show.poster %></td>
        <td><%= show.runtime %></td>
        <td><%= show.network %></td>
        <td><%= show.rating %></td>
        <td><%= show.status %></td>
        <td>
          <div class="btn-group" role="group">
            <%= link_to show, :class => "btn btn-success" do %>
              <span class="glyphicon glyphicon-eye-open"></span>
            <% end %>
            <%= link_to edit_show_path(show), :class => "btn btn-primary" do %>
              <span class="glyphicon glyphicon-pencil"></span>
            <% end %>
            <%= link_to show, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger" do %>
              <span class="glyphicon glyphicon-trash"></span>
            <% end %>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nouvelle série', new_show_path, class: "btn btn-success" %>


